in progress...

For start
```shell script
alias sail="./vendor/bin/sail"
```
Copy env file
```shell script
cp .env.example .env
```
Then for setting up container
```shell script
docker compose up -d
```